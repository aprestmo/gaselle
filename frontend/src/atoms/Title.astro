---
export interface Props {
  content: string;
  tag?: string;
}

const { content, tag = "h2" } = Astro.props as Props;

const Element = tag;
---

<Element>
  <slot name="before-title" />
  {content}
  <slot name="after-title" />
</Element>
